{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Windows Launch",
            "program": "${workspaceFolder}/bin/vulkan_guide.exe",
            "type": "cppvsdbg",
            "request": "launch",
            "cwd": "${workspaceFolder}",
        }
    ]
}